mdformat==1.0.0
mdformat-gfm==1.0.0
markdown-it-py==4.2.0
requests==2.34.2
codespell==2.4.3
yamllint==1.38.0
# Read `_config.yml`'s exclude list in scripts/ci/check_jekyll_liquid.py. Also a
# transitive dependency of yamllint; pinned here so the guard does not rely on
# that staying true.
PyYAML==6.0.3
