This directory is preserved for historical reference only.

- `lua52.dll` is a legacy native dependency that shipped with the old tooling stack.  
- Modern builds do **not** load it; the managed runtime now relies solely on NuGet-delivered dependencies.

