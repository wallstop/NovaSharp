#!/usr/bin/env sh
set -eu

repo_root="$(git rev-parse --show-toplevel 2>/dev/null || printf '')"
if [ -z "$repo_root" ]; then
  printf '%s\n' "Unable to locate repo root for hook." >&2
  exit 1
fi

"$repo_root/scripts/dev/pre-commit.sh"
